# 📈 Stock Forecast Telegram Bot

Telegram-бот для анализа и прогнозирования цен акций с использованием моделей машинного обучения и временных рядов.

## 📋 Оглавление
- [Описание проекта](#описание-проекта)
- [Функциональность](#функциональность)
- [Технический стек](#технический-стек)
- [Архитектура проекта](#архитектура-проекта)
- [Установка и настройка](#установка-и-настройка)
- [Использование](#использование)
- [Логирование](#логирование)
- [Структура проекта](#структура-проекта)
- [Пример работы](#пример-работы)
- [Ограничения и предупреждения](#ограничения-и-предупреждения)
- [Дальнейшее развитие](#дальнейшее-развитие)

## 📊 Описание проекта

Бот позволяет пользователям получать прогнозы цен акций на основе исторических данных с Yahoo Finance. Система автоматически обучает несколько моделей машинного обучения, выбирает наилучшую и предоставляет торговые рекомендации с расчетом потенциальной прибыли.

**Важно:** Проект является учебным и не предоставляет финансовых рекомендаций. Все прогнозы основаны на исторических данных и носят исключительно информационный характер.

## 🚀 Функциональность

- **Поиск тикеров**: Команда `/get_tickers` для поиска акций по букве или части названия
- **Автоматический анализ**: Загрузка исторических данных (2022-2023 гг.) для выбранной акции
- **Множество моделей**: Сравнение трех различных подходов к прогнозированию:
  - Random Forest (классический ML)
  - LSTM (нейросетевая модель)
  - Prophet (статистическая модель от Facebook)
- **Автовыбор модели**: Автоматический выбор лучшей модели на основе метрик качества
- **Прогнозирование**: Построение прогноза на указанное количество дней (от 1 до 365)
- **Торговые сигналы**: Выявление точек покупки и продажи на основе прогноза
- **Расчет прибыли**: Симуляция торговой стратегии с расчетом потенциальной доходности
- **Визуализация**: Графики с историческими данными и прогнозом
- **Логирование**: Подробное логирование всех запросов пользователей

## 🛠 Технический стек

- **Python 3.9+**
- **Библиотеки для бота**:
  - `python-telegram-bot` - фреймворк для Telegram ботов
  - `python-dotenv` - управление переменными окружения
- **Анализ данных**:
  - `yfinance` - загрузка данных с Yahoo Finance
  - `pandas`, `numpy` - обработка данных
  - `matplotlib`, `seaborn` - визуализация
- **Модели машинного обучения**:
  - `scikit-learn` - Random Forest
  - `tensorflow` - LSTM нейросеть
  - `prophet` - модель временных рядов от Facebook
- **Дополнительно**:
  - `scipy` - анализ сигналов
  - `statsmodels` (опционально) - дополнительные статистические методы

## 🏗 Архитектура проекта

Проект построен по модульному принципу:

```
stock_forecast_bot/
├── bot.py                      # Точка входа
├── config.py                   # Конфигурация
├── requirements.txt            # Зависимости
├── .env                       # Переменные окружения
├── .gitignore
├── README.md
│
├── core/                       # Логика бота
│   ├── __init__.py
│   ├── bot_handlers.py        # Обработчики команд
│   ├── states.py              # Состояния диалога
│   └── keyboards.py           # Клавиатуры
│
├── data/                       # Работа с данными
│   ├── __init__.py
│   ├── loader.py              # Загрузка данных Yahoo Finance
│   ├── splitter.py            # Разделение данных
│   └── tickers.json           # Список популярных тикеров
│
├── models/                     # Модели машинного обучения
│   ├── __init__.py
│   ├── base_model.py          # Базовый класс модели
│   ├── ml_model.py            # Random Forest
│   ├── nn_model.py            # LSTM
│   ├── prophet_model.py       # Prophet
│   └── model_selector.py      # Выбор лучшей модели
│
├── analytics/                  # Аналитика и прогнозирование
│   ├── __init__.py
│   ├── forecaster.py          # Построение прогноза
│   ├── visualizer.py          # Визуализация графиков
│   └── strategy.py            # Торговая стратегия
│
├── utils/                      # Вспомогательные функции
│   ├── __init__.py
│   ├── logger.py              # Логирование запросов
│   └── formatters.py          # Форматирование вывода
│
├── logs/                       # Логи (создается автоматически)
│   └── requests_log.csv
│
├── tmp/                        # Временные файлы (создается автоматически)
│   └── *.png                   # Графики прогнозов
│
└── scripts/                    # Вспомогательные скрипты
    └── update_tickers.py       # Обновление списка тикеров
```

## ⚙️ Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd <необходимый путь>/terms_paper
```

### 2. Создание виртуального окружения

```bash
python -m venv .venv

# Активация на Windows:
venv\Scripts\activate

# Активация на Linux/Mac:
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка бота в Telegram

1. Откройте Telegram
2. Найдите @BotFather
3. Создайте нового бота командой `/newbot`
4. Следуйте инструкциям и получите токен
5. Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_от_BotFather
```

### 5. Настройка периода данных (опционально)

По умолчанию используются данные за 2022 год. Вы можете изменить период в `config.py`:

```python
START_DATE = '2022-01-01'
END_DATE = '2023-01-01'
```

### 6. Обновление списка тикеров (опционально)

```bash
python scripts/update_tickers.py
```

## 🤖 Использование

### Запуск бота

```bash
python bot.py
```

### Команды в Telegram

1. **`/start`** - начать анализ акций
   - Введите тикер акции (например, `AAPL`)
   - Введите сумму для инвестиции (например, `1000`)
   - Введите период прогноза в днях (например, `30`)

2. **`/get_tickers [буква]`** - поиск тикеров
   - `/get_tickers A` - тикеры на букву A
   - `/get_tickers AAPL` - поиск по части названия

3. **`/help`** - справка по командам

4. **`/cancel`** - отмена текущего анализа

### Пример диалога

```
Пользователь: /start
Бот: Привет! Я бот для анализа и прогнозирования акций...
Введите тикер компании (например, AAPL, GOOGL, TSLA):

Пользователь: AAPL
Бот: Отлично! Тикер: AAPL
Загружаю исторические данные...
Данные успешно загружены! Период: 252 дней.
Введите сумму для инвестиции:

Пользователь: 1000
Бот: Сумма инвестиции: $1,000.00
Введите период прогнозирования в днях:

Пользователь: 30
Бот: 🔍 Начинаю анализ...
(через несколько минут)
📈 **График прогноза**
📊 **Отчет с рекомендациями**
```

## 📊 Логирование

Все запросы пользователей сохраняются в `logs/requests_log.csv`:

| Поле | Описание |
|------|----------|
| `user_id` | ID пользователя в Telegram |
| `timestamp` | Дата и время запроса |
| `ticker` | Тикер акции |
| `amount` | Сумма инвестиции |
| `forecast_days` | Период прогноза |
| `best_model` | Выбранная модель |
| `metric_value` | Значение метрики RMSE |
| `profit` | Расчетная прибыль (%) |

## 🔍 Структура проекта (подробно)

### Основные модули:

1. **`bot.py`** - главный файл запуска бота, настройка обработчиков
2. **`config.py`** - конфигурационные параметры (пути, настройки моделей)
3. **`core/`** - логика взаимодействия с пользователем:
   - `bot_handlers.py` - обработчики команд и сообщений
   - `states.py` - состояния диалога (FSM)
4. **`data/`** - работа с данными:
   - `loader.py` - загрузка с Yahoo Finance
   - `splitter.py` - разделение на train/test
   - `tickers.json` - список популярных тикеров
5. **`models/`** - модели машинного обучения:
   - `ml_model.py` - Random Forest с лаговыми признаками
   - `nn_model.py` - LSTM нейросеть
   - `prophet_model.py` - модель Prophet
   - `model_selector.py` - сравнение и выбор лучшей модели
6. **`analytics/`** - анализ и визуализация:
   - `forecaster.py` - построение прогноза
   - `visualizer.py` - создание графиков
   - `strategy.py` - торговые сигналы и расчет прибыли

## 📈 Пример работы

### Входные данные:
- Тикер: `AAPL` (Apple Inc.)
- Сумма: `$1,000`
- Период: `30 дней`

### Выходные данные:
1. **График**: Исторические данные + прогноз на 30 дней
2. **Отчет**:
   - Текущая цена: `$190.73`
   - Прогноз через 30 дней: `$205.42`
   - Изменение: `+7.7%`
   - Лучшая модель: `Prophet`
   - Метрики: `RMSE=8.95`, `MAPE=3.83%`
   - Сигналы: `3 покупки`, `2 продажи`
   - Прибыль: `$77.00 (+7.7%)`

## ⚠️ Ограничения и предупреждения

### Ограничения:
1. **Исторические данные**: Используется период 2022-2023 гг.
2. **Доступность данных**: Некоторые тикеры могут быть недоступны
3. **Время анализа**: Обучение моделей занимает 20 сек -- 1.5 минуты
4. **Точность прогнозов**: Прогнозы основаны на исторических данных и не гарантируют точность

### Предупреждения:
- ❌ **Не является финансовым советом**
- ❌ **Не гарантирует прибыль**
- ❌ **Для учебных целей только**
- ⚠️ **Рыночные риски**: Цены акций могут изменяться непредсказуемо
- ⚠️ **Ограничения моделей**: Все модели имеют погрешности

## 🔮 Дальнейшее развитие

### Планируемые улучшения:
1. **Дополнительные модели**:
   - XGBoost, LightGBM
   - Трансформеры для временных рядов
   - Ансамбли моделей

2. **Расширенный анализ**:
   - Технические индикаторы (RSI, MACD, Moving Averages)
   - Анализ волатильности
   - Корреляция с индексами рынка

3. **Функциональность бота**:
   - Подписка на уведомления
   - Портфель пользователя
   - Сравнение нескольких акций
   - Экспорт отчетов в PDF

4. **Оптимизация**:
   - Кэширование моделей
   - Асинхронная загрузка данных
   - Оптимизация гиперпараметров

5. **Интерфейс**:
   - Веб-интерфейс для администрирования
   - Мобильное приложение
   - API для внешних систем

- **Автор**: Студент М24-525 Смородин Борис Борисович

**Важное напоминание**: Этот бот предназначен исключительно для образовательных целей. Все финансовые решения должны приниматься на основе консультации с профессиональными финансовыми советниками. Авторы не несут ответственности за любые финансовые потери, возникшие в результате использования этого программного обеспечения.